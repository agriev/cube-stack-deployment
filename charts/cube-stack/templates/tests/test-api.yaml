{{- if and .Values.tests.enabled .Values.api.enabled }}
apiVersion: v1
kind: Pod
metadata:
  name: {{ include "cubeStack.fullname" . }}-test-api
  namespace: {{ .Release.Namespace }}
  labels: {{- include "cubeStack.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: test
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: curl
      image: "{{ .Values.tests.image.repository }}:{{ .Values.tests.image.tag }}"
      imagePullPolicy: {{ .Values.tests.image.pullPolicy }}
      command:
        - /bin/sh
        - -c
        - |
          set -e
          echo "→ /livez"
          curl -fsS "http://{{ include "cubeStack.api.fullname" . }}:{{ .Values.cube.apiPort }}/livez"
          echo
          echo "→ /readyz"
          curl -fsS "http://{{ include "cubeStack.api.fullname" . }}:{{ .Values.cube.apiPort }}/readyz"
          echo
          echo "OK"
{{- end }}
